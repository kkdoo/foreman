start on starting <%= app %>-<%= name %>
stop on stopping <%= app %>-<%= name %>
respawn

env PORT=<%= port %>
<% engine.env.each_pair do |var,env| -%>
env <%= var.upcase %>=<%= shell_quote(env) %>
<% end -%>

exec su - <%= user %> --session-command 'cd <%= engine.root %>; <% end %> <%= process.command %> >> <%= log %>/<%=name%>-<%=num%>.log 2>&1'
